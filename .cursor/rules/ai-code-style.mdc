---
description: AI 生成或修改代码时的注释与作者规范（kudos 仓库）
alwaysApply: true
---

# 代码注释与作者

## 作者

凡由 AI 创建或实质修改的源文件，在 Javadoc/KDoc 的 `@author` 中须包含 **`@author AI: Cursor`**（可与已有 `@author` 并存，例如先写人类作者再写 AI）。

## 注释内容

- **禁止**在注释中描述对话过程、任务背景、「按用户要求」「之前」「后来」等与代码无关的叙述。
- **应写**当前代码的用途、行为、前置/后置条件、重要参数与返回值含义，便于后续维护者直接阅读。
- 类、接口、公开 API（含 Controller 暴露的端点）**必须有**类级与方法级说明；非平凡的 private 方法也应有简要说明。

## 语言

与项目现有代码一致，中文说明可接受时保持与周边文件统一。

## 公开方法（public / 对外可见 API）

- **每个**对外可见的方法（如 `public`、接口方法、Controller 端点、Service 对外 API）的 KDoc/Javadoc **必须**包含：
  - **入参**：每个参数的含义（必要时说明 null/空串语义、单位、默认值）。
  - **出参**：返回值含义；可空类型须说明何时为 `null`；集合/分页须说明元素类型或结构。
- 无参且无返回值（`Unit`/`void`）时，仍须说明方法副作用（例如刷新哪类缓存、抛何种业务异常）。

## 单元测试与数据库造数（SQL）

- 需要随测试加载的 SQL 脚本放在**被测模块**的 **`test-resources`** 下与资源约定一致的子目录中（例如 `test-resources/sql/h2/...`，与现有测试一致）。
- **SQL 文件名与单元测试类名一致**，例如类 `FooTest` 对应 `FooTest.sql`；类级 KDoc 中注明数据来源文件名（与项目惯例一致）。
- **文件级独立性**：该 SQL 内数据应自洽，不依赖其它测试 SQL 的执行顺序或其它文件插入的中间状态；所需表数据在本文件内可重复执行或完整描述（与项目现有 `merge`/`insert` 风格一致即可）。
- **造数前**先到 **`kudos-ms-sys-sql`**（或对应库的 sql 模块）中查阅 **建表脚本 / Flyway 版本脚本**，确认：
  - 字段类型、长度、是否非空；
  - 主键、**唯一约束**、外键、检查约束；
  - 与业务相关的默认值、枚举/字典码取值。
- 所造数据 **必须符合** 上述表定义与约束，避免仅因测试数据非法导致用例失败。

## 字典字段命名（`*_dict_code`）

- 名称以 **`_dict_code`** 结尾的列（或 Kotlin/Java 中对应的 `xxxDictCode` 属性）表示：存的是 **`sys_dict_item.item_code`**。
- 解析该字典时，**`sys_dict.dict_type`** 与字段名的对应关系为：**去掉 `_dict_code` 后缀后剩余部分**（数据库列示例：`rule_type_dict_code` → 字典类型为 `rule_type`；属性名 `ruleTypeDictCode` 语义同上，文档中可写「对应 `dict_type = rule_type`」）。
- 实现或造数时，须在 `sys_dict` / `sys_dict_item` 中存在匹配的 `dict_type` 与 `item_code`，避免悬空字典码。

## 国际化 key（`sys_i18n`）

- 完整国际化缓存 key 为 **四段** 由英文 `.` 连接，顺序固定：
  `atomic_service_code` **`.`** `i18n_type_dict_code` **`.`** `namespace` **`.`** `key`
- 四段取值均来自 `sys_i18n` 表（或与其一致的语义），拼接后的整串才是参与缓存/查找的 **完整 i18n key**；文档与注释中应说明「四段点分」规则，避免只写局部片段。
