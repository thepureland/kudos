kudos:
  ability:
    web:
      url:
        suffix: .html
      springmvc:
        # Left unset so the default test context exercises the same path production does: Spring Boot
        # selects the container from the classpath. The container-specific test classes
        # (TomcatServerTest / JettyServerTest) set it explicitly to force their respective container.
