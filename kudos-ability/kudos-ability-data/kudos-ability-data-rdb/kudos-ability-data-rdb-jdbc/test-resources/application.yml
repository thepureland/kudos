# Disable the kudos cache autoconfig in this module's tests: kudos-ability-cache-common is on the
# test classpath (so DataSourceClearListenerTest can exercise CacheCleanRegister), but neither
# cache-local-caffeine nor cache-remote-redis are — so LinkableCacheAutoConfiguration would fail
# wiring localCacheManager. Cache-driven datasource refresh is covered separately by the unit test.
kudos:
  ability:
    cache:
      enabled: false

spring:
#  sql:
#    init:
#      schema-locations: sql/schema.sql
  datasource:
    dynamic:
      primary: ds2
      hikari:
        connection-timeout: 30000
        validation-timeout: 5000
        idle-timeout: 600000
        leak-detection-threshold: 0
        max-lifetime: 1800000
        max-pool-size: 10
        min-idle: 10
        initialization-fail-timeout: 1
        is-auto-commit: false
        is-read-only: false
        is-isolate-internal-queries: false
        is-register-mbeans: false
        is-allow-pool-suspension: false
      datasource:
        master:
          hikari:
            is-auto-commit: false
          driver-class-name: org.h2.Driver
          url: jdbc:h2:~/h2/ds-master;DATABASE_TO_LOWER=TRUE;
          username: sa
          password: sa
          init:
            schema: sql/schema.sql
        ds1:
          driver-class-name: org.h2.Driver
          url: jdbc:h2:~/h2/ds1;DATABASE_TO_LOWER=TRUE;
          username: sa
          password: sa
          init:
            schema: sql/schema.sql
#            data: sql/data.sql
        ds2:
          driver-class-name: org.h2.Driver
          url: jdbc:h2:~/h2/ds2;DATABASE_TO_LOWER=TRUE;
          username: sa
          password: sa
          init:
            schema: sql/schema.sql
#            data: sql/data.sql

