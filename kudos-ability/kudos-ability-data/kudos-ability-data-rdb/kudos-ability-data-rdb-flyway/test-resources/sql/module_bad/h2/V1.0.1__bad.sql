-- Intentionally invalid SQL: used by FlywayKitTest to exercise the failure/rethrow branch.
-- This module is NOT declared under kudos.ability.flyway.datasource-config, so in manual mode
-- the multi-data-source migrator silently ignores it (on-disk but unconfigured).
THIS IS NOT VALID SQL AT ALL !!!;
