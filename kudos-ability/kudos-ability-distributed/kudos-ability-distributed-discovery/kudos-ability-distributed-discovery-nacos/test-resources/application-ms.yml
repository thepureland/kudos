server:
  port: 0

spring:
  application:
    name: discovery
  cloud:
    # The Spring Cloud release train does not yet declare compatibility with Spring Boot 4.1.x;
    # the verifier would otherwise abort context startup in tests.
    compatibility-verifier:
      enabled: false
    nacos:
      discovery:
        server-addr: 127.0.0.1:28848
        register-enabled: true         # 确保开启自动注册
        ip: 127.0.0.1                  # 指定注册到 Nacos 的 IP


#logging:
#  level:
#    root: DEBUG
#    com.alibaba.cloud: DEBUG