# kudos-ms-sys-common

系统（sys）域的公共契约与数据结构定义。

## 主要内容

- `io.kudos.ms.sys.common.api`
  - 领域接口定义（ISys*）
- `io.kudos.ms.sys.common.vo`
  - VO / payload / record / cache item

## 用途

供 `core`、`client` 与各 API 模块共享。
