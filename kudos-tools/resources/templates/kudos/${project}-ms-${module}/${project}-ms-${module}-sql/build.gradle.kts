// Flyway migration scripts only (resources/sql/${module}/<dialect>/V*.sql); no source code, no dependencies.
// The scripts are executed at runtime by kudos-ability-data-rdb-flyway, which the core module brings in.
dependencies {
    //region your codes 1

    //endregion your codes 1
}
