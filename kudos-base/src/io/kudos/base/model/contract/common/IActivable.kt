package io.kudos.base.model.contract.common

/**
 * Activatable (carrying an active property) model interface.
 *
 * @author K
 * @since 1.0.0
 */
interface IActivable {

    /** Whether the entry is active */
    var active: Boolean

}
